<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Campus English Hub</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body>

  <% 
    // ✅ Use the correct variable passed from server.js
    const isAdminPage = typeof currentPath !== 'undefined' && currentPath.startsWith('/admin');
  %>

  <% if (!isAdminPage) { %>
  <!-- Navbar (hidden on admin pages) -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-3">
    <div class="container">
      <a class="navbar-brand fw-bold text-primary fs-3" href="/">Campus English Hub</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><a href="/" class="nav-link px-3">Home</a></li>
          <li class="nav-item"><a href="/lessons" class="nav-link px-3">Lessons</a></li>
          <li class="nav-item"><a href="/quiz" class="nav-link px-3">Quiz</a></li>
          <li class="nav-item"><a href="/about" class="nav-link px-3">About</a></li>
        </ul>
      </div>
    </div>
  </nav>
  <% } %>

  <main class="content">
